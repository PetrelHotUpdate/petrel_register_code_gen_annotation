name: petrel_register_code_gen_annotation
description: 雨燕热更新注册代码生成注解
version: 0.0.1
repository: https://github.com/PetrelHotUpdate/petrel_register_code_gen_annotation

environment:
  sdk: ^3.5.4

# Add regular dependencies here.
# dependencies:
# path: ^1.8.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
